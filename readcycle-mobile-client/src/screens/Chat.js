import { StatusBar } from "expo-status-bar";
import { Text, View } from "react-native";

export default function Chat() {
	return (
		<View className="my-auto mx-auto">
			<Text className="">This is Landing Page</Text>
			<StatusBar style="auto" />
		</View>
	);
}
